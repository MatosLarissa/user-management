export default interface UserData {
    id: string;
    name: string;
    birthDate: string;
    email: string;
    password: string;
    createdAt: string;
    updatedAt: string;
    lastLogin: string;
}